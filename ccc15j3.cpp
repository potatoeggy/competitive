#include <iostream>
#include <vector>
#include <string.h>
#include <stdio.h>

std::vector<char> piglatin;
std::string string;
char stringButArray[31];
const std::string array[] = {"bc", "dfg", "hjkl", "mnpqr", "stvwxyz"};
int main() {
	std::cin.sync_with_stdio(0);
	std::cin.tie(0);

	std::cin >> string;
	std::strncpy(); // why is this the harder part
	for (int i = 0; i < string.length(); i++) {
		string.
	}
	return 0;
}