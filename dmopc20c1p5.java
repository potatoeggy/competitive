import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.io.IOException;
import java.util.StringTokenizer;
import java.util.ArrayList;

public class dmopc20c1p5 {
	static BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
	static StringTokenizer st;

	static int nextInt() throws IOException {
		while (st == null || !st.hasMoreTokens()) {
			st = new StringTokenizer(br.readLine());
		}
		return Integer.parseInt(st.nextToken());
	}

	public static void main(String[] args) {
		ArrayList<Integer>[] adj = new ArrayList[nextInt()];
		ArrayList<Integer>[] assignments = new ArrayList[adj.length];
		for (int i = 0; i < adj.length; i++) {
			adj[i] = new ArrayList<Integer>();
			assignments[i] = new Arra // the question no make sense i'm gonna do mock ia now
		}

		int aliens = nextInt(); int activities = nextInt();

		for (int i = 0; i < adj.length; i++) {
			assign
		}
	}
}